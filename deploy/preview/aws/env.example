AWS_PROFILE=atrium-preview
AWS_REGION=us-east-1

# Defaults are intentionally sized for the first reliable spike, not minimum cost.
ATRIUM_PREVIEW_INSTANCE_TYPE=t3a.xlarge
ATRIUM_PREVIEW_VOLUME_GB=160
ATRIUM_PREVIEW_TTL_HOURS=24
