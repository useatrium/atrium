# Authentication is handled by iron-proxy's oauth_token transform. The proxy
# refreshes the Google OAuth credential and injects a short-lived access
# token on outbound Google API requests. The tool itself needs no credentials.
#
# The credential lives in the GOOGLE_TOKEN_JSON secret (see
# [tool.ai-v2].secrets in pyproject.toml).
